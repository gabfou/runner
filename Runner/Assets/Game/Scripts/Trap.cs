using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Trap : MonoBehaviour
{
    public bool[] CanPutPiece = new bool[6]; // dont change the size, only work with 3 rail, the tree first represent the low row from left to right the tree last represent the hight row from left to right
    
    void Start()
    {
        
    }

    
    void Update()
    {
        
    }
}
